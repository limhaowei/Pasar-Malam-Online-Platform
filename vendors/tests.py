from django.test import TestCase

"""
1. normal user (cannot access admin page, and cannot edit any vendor page)
2. register as a vendor (can edit their OWN vendor page)
2. superuser (only group that can see and access admin page. also can delete vendors)
"""
